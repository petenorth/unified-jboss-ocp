#!/bin/bash
set -eo pipefail
echo
echo
echo "RUNNING THE BLOODY SCRIPT"
echo
echo
set -x
if [ x"$REFERENCE_DATA_DIRECTORY" != "x" ]; then
    mkdir -p $REFERENCE_DATA_DIRECTORY
    cp /tmp/src/refdataloader/src/main/resources/ref/* $REFERENCE_DATA_DIRECTORY
fi
set +x

exec /usr/local/s2i/run
